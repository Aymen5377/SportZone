name: Full Automated EAS Build
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout Repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 📦 Install EAS CLI
        run: npm install -g eas-cli

      - name: 🛠️ Create EAS Config File
        run: |
          echo '{
            "build": {
              "preview": {
                "android": {
                  "buildType": "apk"
                }
              }
            }
          }' > eas.json

      - name: 🔗 Configure EAS Project (Auto-Init)
        # هذا السطر سيقوم بربط المشروع بحسابك تلقائياً دون تدخل منك
        run: npx eas-cli init --name SportZone --slug SportZone --non-interactive
        env:
          EXPO_TOKEN: "V8zgDeYnZ6nUPq2C14BzBTNZCesKRBVQBYit0HML"

      - name: 📦 Install Dependencies
        run: npm install --legacy-peer-deps

      - name: 🚀 Start EAS Build (APK Production)
        run: eas build --platform android --profile preview --non-interactive
        env:
          EXPO_TOKEN: "V8zgDeYnZ6nUPq2C14BzBTNZCesKRBVQBYit0HML"
          
